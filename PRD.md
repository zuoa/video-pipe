# 视频流转换网关 - 架构设计文档

> 项目定位演进：从「任意视频源转RTSP」扩展为「video-pipe：多协议流格式互转网关」，当前阶段聚焦 **MVP：简单网关 + Web 管理界面**，暂不涉及内容处理/分析。

## 0. 当前阶段范围（MVP）

**做什么**：
- 接收任意输入源（文件/RTMP/HTTP/RTSP/摄像头等）
- 转换为多种流协议输出（RTSP/RTMP/FLV/HLS/WebRTC 等）
- 提供 Web 界面管理流的增删查、查看播放地址、查看状态

**不做什么（后续阶段再考虑）**：
- 不做流内容处理（水印、转码分辨率、AI分析插帧等）
- 不做复杂鉴权/多租户
- 不自己实现协议转换逻辑（复用 MediaMTX）

---

## 1. 项目目标

将任意类型的视频输入源（本地文件、RTMP/HTTP直播流、摄像头/RTSP设备、GB28181国标设备等）统一转换为标准流协议输出（RTSP/RTMP/FLV/HLS/WebRTC），供下游播放器/客户端或分析系统消费。

---

## 2. 整体架构

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐    ┌──────────┐
│  输入源适配层 │ →  │  转码/转封装层 │ → │  RTSP 服务层 │ → │  客户端   │
│ (文件/RTMP/  │    │  (FFmpeg)     │    │ (MediaMTX)  │    │ (VLC/    │
│  HTTP/摄像头/│    │               │    │             │    │  播放器) │
│  拉流协议等) │    │               │    │             │    │          │
└─────────────┘    └──────────────┘    └─────────────┘    └──────────┘
       ↑                                       ↑
   任务调度/管理层 ←──────────────────────────────┘
   (任务队列、健康检查、自动重启、按需启停)
```

### 分层职责

| 层 | 职责 | 技术选型 |
|---|---|---|
| 输入源适配层 | 识别/规范化各类输入源地址和协议 | 自研（元数据+协议判断） |
| 转码/转封装层 | 将输入流转为 RTSP 兼容格式 | FFmpeg（子进程） |
| RTSP 服务层 | 对外提供 RTSP/RTMP/HLS/WebRTC 播放地址，多播放端共享一路流 | MediaMTX |
| 调度管理层 | 任务生命周期管理、健康检查、按需启停、API | **待选（本文档核心决策点）** |

---

## 3. 核心组件选型

### 3.1 RTSP 服务核心：MediaMTX
- 单二进制部署，支持 RTSP/RTMP/HLS/WebRTC 网关
- 两种接入模式：
  - **推流模式**：外部 FFmpeg 主动 `ffmpeg -f rtsp` 推给 MediaMTX
  - **拉流发布模式**：配置 `runOnDemand`，让 MediaMTX 按需拉起 FFmpeg 拉源，无人观看自动停止（省资源）
- 自带 HTTP API，可动态增删 path、查询流状态，减少自研管理逻辑

### 3.2 转码/转封装：FFmpeg
- 处理输入源多样性的核心工具，几乎覆盖所有输入协议/格式
- **优先无损转封装**（不转码，节省 CPU）：
  ```bash
  ffmpeg -re -i <输入源> -c:v copy -c:a aac -f rtsp rtsp://mediamtx:8554/mystream
  ```
- 仅当编码不兼容（如源不是 H.264/H.265）或需要转分辨率/码率时才真正转码
- 大规模部署时优先用 GPU 硬件编码（NVENC/QSV）降低 CPU 压力

### 3.3 特殊输入源
- **GB28181 国标设备**：需要 SIP 信令层，不能直接 FFmpeg 拉流，建议接入开源网关（如 wvp-GB28181-pro）先转成标准 RTSP，再进本系统
- **ONVIF 摄像头**：先做设备发现拿到 RTSP 地址，再走标准流程

---

## 4. 调度管理层设计（需自研部分）

这是本项目真正的开发工作量所在，MediaMTX + FFmpeg 都是现成组件。

### 4.1 核心数据模型
```
Stream Task {
  id: string
  source_url: string        // 输入源地址
  source_type: enum         // file / rtmp / http / rtsp / onvif / gb28181
  target_path: string       // MediaMTX 上的发布路径
  transcode_params: object  // 转码参数，null 表示纯转封装
  status: enum              // pending / running / error / stopped
  on_demand: bool           // 是否按需启停
}
```

### 4.2 核心能力
1. **进程管理**：每路流对应一个独立 FFmpeg 子进程，通过看门狗监控存活状态
2. **健康检查**：定期检查 FFmpeg 进程存活 + MediaMTX 侧流是否有数据在更新
3. **自动重启**：异常退出的流按退避策略（backoff）自动重启，避免死循环拉起
4. **按需启停**：结合 MediaMTX 的 `runOnDemand` / `runOnUnDemand` hook，实现有人拉流才启动转码、无人观看自动停止
5. **管理 API**：增删查改流任务、查看实时状态

### 4.3 隔离策略
- **单路一进程**：每路流独立 FFmpeg 进程，隔离性好、可单独重启/扩容
- 推荐用 Docker 容器封装每路流（或至少用进程组隔离），便于资源限制和回收

---

## 5. 部署形态建议

```
用户配置输入源 
  → 后端下发任务
  → 启动 FFmpeg 进程/容器（-re -i 源 -c copy -f rtsp 推给 MediaMTX）
  → MediaMTX 对外提供统一 RTSP/RTMP/HLS/WebRTC 播放地址
```

- 中小规模：docker-compose 编排「调度服务 + MediaMTX + N 个 FFmpeg 容器」
- 大规模：K8s 编排，转码节点水平扩展 + 负载均衡；转码是 CPU/GPU 密集型，是主要的横向扩展点

---

## 6. 调度层编程语言选型

**推荐：Go**

理由：
- 调度层的本质工作是「进程管理 + 高并发轻量任务（N 路健康检查）+ HTTP API」，不是计算密集型
- goroutine 天然适合管理大量并发子进程和检查任务
- `os/exec` + `context` 写子进程管理和超时控制很顺手
- 和 MediaMTX 同语言，生态贴近，必要时可以直接参考/魔改其源码
- 编译单二进制，部署简单

备选：
- **Python**：团队 Python 栈或需要结合 AI/CV 处理时选用，配合 FastAPI + asyncio/Celery
- **Node.js**：管理界面和调度逻辑想共用一套技术栈时选用
- **Rust**：如果未来想深入自己实现协议栈/媒体处理逻辑（而不是拼装 MediaMTX+FFmpeg），或部署在资源极度受限的边缘设备，值得考虑；但纯调度层场景下开发效率不如 Go

**不建议**自己用 C/C++ 重写转发核心——MediaMTX 已经把这块做得很成熟，重复造轮子性价比低。

---

## 7. 关键设计决策速查表

| 决策点 | 建议 |
|---|---|
| 转码放哪 | 优先 `-c copy` 无损转封装，不兼容才转码 |
| 单路一进程还是多路一进程 | 单路一进程 |
| 部署形态 | Docker 容器化每路 FFmpeg + 独立 MediaMTX，K8s/compose 编排 |
| 扩展性 | GPU 硬件编码 + 转码节点水平扩展 + 负载均衡 |
| 输入源鉴权/协议适配 | 摄像头走 ONVIF 发现；国标设备走 wvp-GB28181-pro 网关转 RTSP 后接入 |
| 调度层语言 | Go（默认推荐），Python/Node 视团队栈而定，Rust 视是否深入协议层而定 |

---

## 8. 待确认信息（影响细节设计）

在开始 vibe coding 前，建议先明确：
- [ ] 输入源主要类型：摄像头 / 文件 / 直播流 / 国标设备？
- [ ] 并发路数量级：几十路还是几百上千路？
- [ ] 是否需要真正转码，还是大部分场景可以纯转封装？
- [ ] 部署环境：云上还是边缘设备？资源限制情况？
- [ ] 是否需要鉴权、多租户隔离？

---

## 9. video-pipe 定位：多协议互转网关

后续演进方向：不局限于"转RTSP"，而是做通用的 **N种输入协议 → M种输出协议** 转换网关。

### 9.1 转换核心思路

不自己写 N×M 组合的转换代码，而是走"统一中间层"模式：

```
输入源(N种协议) → 统一解封装/接收 → 内部标准媒体流(帧+时间戳+编码格式) → 按需重新封装(M种协议) → 输出
```

FFmpeg/GStreamer/现成网关软件本身就是这个模式的实现，video-pipe 的自研部分应该是**调度和管理层**，而不是重新实现解封装/封装逻辑。

### 9.2 核心组件选型对比

| 方案 | 特点 | 适用 |
|---|---|---|
| **MediaMTX** | Go，原生支持 RTSP/RTMP/HLS/WebRTC/SRT 互转，HTTP API 动态管理，单二进制部署 | 首选，标准协议覆盖全，运维简单 |
| **ZLMediaKit** | C++，对 RTMP/RTSP/FLV/HLS/GB28181 支持成熟，国内摄像头/国标生态兼容性更好 | 涉及国标设备/国内摄像头厂商较多时 |
| **SRS** | 专注 RTMP/HLS/WebRTC，直播场景成熟度高 | 偏直播分发场景 |

**MVP 阶段结论：直接用 MediaMTX，不自己实现协议转换。**

### 9.3 处理扩展点（后续阶段，非MVP）

若未来要在转换过程中插入自定义处理（水印、转码、AI分析等），需要引入 GStreamer 的插件化管道能力，通过 `appsink`/`appsrc` 把帧交给自定义代码处理后再送回 pipeline。MVP 阶段不涉及此部分。

---

## 10. MVP 架构：简单网关 + Web 管理界面

### 10.1 架构图

```
┌─────────────┐    ┌─────────────┐    ┌──────────────┐
│  Web 前端     │ ←→ │  后端 API    │ ←→ │  MediaMTX    │
│ (流管理界面)  │    │ (Go/Python) │    │ (转换核心)    │
└─────────────┘    └─────────────┘    └──────────────┘
                          │
                          ↓
                    数据库(SQLite/Postgres)
                    存流配置和元数据
```

**核心原则**：MediaMTX 原生支持多协议互转，后端只做"管理 MediaMTX 配置 + 调用其 HTTP API"的薄封装，不重复实现转换逻辑。流的实时状态直接查 MediaMTX，不在自己数据库里维护。

### 10.2 组件分工

**MediaMTX（核心，直接用现成的）**
- 一路输入进来，同时以 RTSP/RTMP/HLS/WebRTC 多格式输出，多播放端共享同一路流
- 自带 HTTP API（默认端口 9997），支持运行时动态增删 path，无需重启

**后端 API（自研，职责很薄）**

| 接口 | 功能 |
|---|---|
| `POST /streams` | 新增一路流（输入源） → 调 MediaMTX API 创建 path |
| `GET /streams` | 列出所有流及状态 |
| `DELETE /streams/:id` | 删除一路流 |
| `GET /streams/:id/urls` | 返回该流各格式播放地址（rtsp://、rtmp://、.../index.m3u8） |
| `GET /streams/:id/status` | 查询 MediaMTX 流状态（在线/离线/是否有数据） |

**Web 前端（管理界面）**
- 表单：填输入源地址 + 流名称 → 提交创建
- 列表：所有流 + 状态灯（绿/红）+ 各格式播放地址（便于复制到 VLC 测试）
- 一键删除/停止
- 加分项：内嵌播放器直接预览（HLS.js 播放 HLS 输出，或 WebRTC 播放器）

### 10.3 数据模型（最小版）

```
Stream {
  id: uuid
  name: string          // 流标识，对应 MediaMTX path 名
  source_url: string    // 输入源，如 rtsp://... / rtmp://... / 本地文件路径
  created_at: timestamp
}
```

不需要存储"输出格式"字段——MediaMTX 默认全格式输出，前端直接按固定规则拼出各协议播放地址即可。

### 10.4 技术栈建议

| 层 | 建议 | 说明 |
|---|---|---|
| 后端 | Go 或 Python FastAPI | Go 更贴近 MediaMTX 生态、部署简单；FastAPI 开发更快，看团队熟悉度 |
| 前端 | React/Vue，或先用后端模板渲染 | 追求极简可先用 Go html/template 或 Jinja2，省去前后端分离成本 |
| 数据库 | SQLite | 流数量不大时够用，无需引入 Postgres |
| 部署 | docker-compose | MediaMTX + 后端 + 前端（或前后端一体）三件套 |

### 10.5 建议开发顺序

1. 先跑通 MediaMTX 单独部署，手动改配置加一路流，验证多格式输出都能播放
2. 写后端最小 API：创建流（调 MediaMTX API）+ 查询流列表
3. 最简单的 HTML 管理页面：提交表单 + 列表展示
4. 补状态检查（轮询 MediaMTX 状态接口）
5. 最后再考虑播放器预览、鉴权等锦上添花功能

### 10.6 后续演进路径（非MVP，仅作规划参考）

- 引入更多输入协议适配（SRT、GB28181网关接入）
- 引入处理扩展点（GStreamer + appsink/appsrc，接水印/转码/AI分析）
- 按需启停已实现：MediaMTX `runOnDemand` 唤醒后端 FFmpeg，无人拉流 30 秒后自动停止
- 引入鉴权、多租户、操作审计
- 大规模场景下拆分调度层为独立服务，支持多节点/K8s部署
